const header=document.querySelector('.site-header');const menu=document.querySelector('.menu-button');menu.addEventListener('click',()=>{const open=menu.getAttribute('aria-expanded')==='true';menu.setAttribute('aria-expanded',String(!open));header.classList.toggle('menu-open',!open)});document.querySelectorAll('#site-nav a').forEach(a=>a.addEventListener('click',()=>{menu.setAttribute('aria-expanded','false');header.classList.remove('menu-open')}));
const viewer=document.querySelector('.viewer'),viewerImage=viewer.querySelector('img'),viewerText=viewer.querySelector('p');document.querySelectorAll('.zoom>img').forEach(img=>img.addEventListener('click',()=>{viewerImage.src=img.currentSrc||img.src;viewerImage.alt=img.alt;viewerText.textContent=img.alt;viewer.showModal()}));viewer.querySelector('button').addEventListener('click',()=>viewer.close());viewer.addEventListener('click',e=>{if(e.target===viewer)viewer.close()});
const observer=new IntersectionObserver(entries=>entries.forEach(entry=>{if(entry.isIntersecting){entry.target.classList.add('visible');observer.unobserve(entry.target)}}),{threshold:.07});document.querySelectorAll('.reveal').forEach(el=>observer.observe(el));
